# Do not hard-code a deployment URL here. Set VITE_API_URL in the hosting
# provider's build environment, for example: https://api.example.com/api
# Use /api only when the frontend host reverse-proxies that path to this backend.
