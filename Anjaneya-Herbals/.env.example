# Local development: the Vite app talks directly to the local Spring backend.
VITE_API_URL=http://localhost:8888/api

# Used by Vite's optional same-origin proxy if VITE_API_URL is set to /api.
VITE_API_PROXY_TARGET=http://localhost:8888
